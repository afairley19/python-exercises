# HackerRank Practice: Sock Merchant

def sockMerchant(n, ar):
    
    values = list(set(ar))
    pairs = 0

    for num in values:
        pairs t = ar.count(num) // 2 #int instead of float
    return pairs